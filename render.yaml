services:
  - type: worker
    name: botmsna-factory
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python -m factory.bot
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: ADMIN_ID
        sync: false
      - key: DATABASE_URL
        sync: false
